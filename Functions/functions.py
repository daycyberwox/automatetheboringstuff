# Functions

import random   # importing the random module
ranum = random.randint(1, 10) # picks a random number (integer) between 1 and 10
print(ranum)

import sys, os, math     # importing multiple modules using commas
from random import *     # import everything * from the random module

import sys
print("Hello")
sys.exit()   # terminates the program immediately
print("Goodbye")    # never gets printed because of sys.exit


